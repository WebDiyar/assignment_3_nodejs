<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Currency App using NodeJS</title>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.14.0/css/all.css">
    <style>
        /* Reset default browser styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        /* Styling for the container */
        .container {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            justify-content: center;
            align-items: center;
            background-color: #e9ecef;
            padding: 20px;
        }

        .app-header h1 {
            margin-bottom: 1rem;
            color: #495057;
        }

        .app-content {
            width: 100%;
            max-width: 480px;
            background: #fff;
            padding: 2rem;
            border-radius: 0.5rem;
            box-shadow: 0 0.5rem 1.5rem rgba(0, 0, 0, 0.1);
        }

        .currency-form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .form-group input,
        .form-group button {
            width: 100%;
            padding: 0.75rem;
            border-radius: 0.25rem;
            border: 1px solid #ced4da;
        }

        .form-group button {
            background-color: #007bff;
            color: white;
            border: none;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .form-group button:hover {
            background-color: #0056b3;
        }

        .error-message p {
            color: #dc3545;
            margin-top: 1rem;
        }

        .currency-result {
            margin-top: 1rem;
            background-color: #f8f9fa;
            padding: 1rem;
            border-radius: 0.25rem;
            border-left: 4px solid #007bff;
        }

        .currency-result p {
            margin-bottom: 0.5rem;
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }

            .app-content {
                padding: 1rem;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <header class="app-header">
            <h1>Currency Converter</h1>
        </header>
        <main class="app-content">
            <form action="/currency" method="get" class="currency-form">
                <div class="form-group">
                    <input type="text" name="money" placeholder="Currency ONLY KZT, RUB, EUR" required>
                </div>
                <div class="form-group">
                    <input type="number" name="amount" placeholder="Amount" required>
                </div>
                <div class="form-group">
                    <button type="submit">Convert</button>
                </div>
            </form>
            <% if (error) { %>
                <div class="error-message">
                    <p>
                        <%= error %>
                    </p>
                </div>
                <% } %>
                    <% if (data) { %>
                        <div class="currency-result">
                            <p><strong>Base:</strong>
                                <%= data.base %>
                            </p>
                            <p><strong>Amount:</strong>
                                <%= data.amount %>
                            </p>
                            <p><strong>Result:</strong>
                                <%= data.result.KZT || data.result.RUB || data.result.EUR %>
                            </p>
                            <p><strong>Rate:</strong>
                                <%= data.result.rate %>
                            </p>
                        </div>
                        <% } %>
        </main>
    </div>
</body>

</html>